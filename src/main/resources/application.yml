spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3308/community_feed?serverTimezone=Asia/Seoul&useSSL=false&allowPublicKeyRetrieval=true
    username: service_user
    password: service_user

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    open-in-view: false